# Node modules folder
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment variables (make sure you don't commit sensitive credentials)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build output directories
dist/
build/

# Vercel output directory (if generated)
.vercel/

# Mac system files
.DS_Store

# Optional: If you have a local copy of your Firebase service account key file,
# ensure it is ignored to prevent accidental commits.
firebase-service-account.json
